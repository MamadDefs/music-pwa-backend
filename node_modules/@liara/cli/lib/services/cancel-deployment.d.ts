import { Got } from 'got';
declare const _default: (httpClient: Got, releaseID: string, retryOptions: {
    retries: number;
    onRetry: (error: any, attempt: number) => void;
}) => Promise<void>;
export default _default;
